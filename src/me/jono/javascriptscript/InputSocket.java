package me.jono.javascriptscript;

public class InputSocket extends Socket {
    /**
     * Creates an InputSocket
     * @param node The Node the Socket is attached to
     */
    public InputSocket(Node node, String name) {
        super(node, name);
    }
}
